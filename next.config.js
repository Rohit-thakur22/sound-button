/** @type {import('next').NextConfig} */
const nextConfig = {
  // Remove the old i18n config as we'll handle it with middleware
  // i18n: {
  //   locales: ['en', 'fr', 'de', 'es', 'bn', 'hi', 'ja', 'ko'],
  //   defaultLocale: 'en',
  // },
}

module.exports = nextConfig
  